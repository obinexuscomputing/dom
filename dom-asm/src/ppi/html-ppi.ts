import { PPI, PPIConfig } from './ppi-types';

export class HTMLPPI implements PPI {
  process(input: string, config: PPIConfig): string {
    // Implementation will be added
    return input;
  }
}