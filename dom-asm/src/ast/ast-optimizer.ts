import { ASTNode, ASTOptimizer } from './ast-types';

export class HTMLASTOptimizer implements ASTOptimizer {
  optimize(node: ASTNode): ASTNode {
    // Implementation will be added
    return node;
  }
}
